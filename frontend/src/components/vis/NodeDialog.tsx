import { useEffect } from "react";
import { useAppSelector } from "../../store";

export default function NodeDialog(): JSX.Element {

  const { } = useAppSelector((state) => state.vis);

  useEffect(() => {

  }, []);

  return (
    <></>
  );
}
